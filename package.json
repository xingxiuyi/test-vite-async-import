{
  "name": "root",
  "private": true,
  "author": "XingXiuyi",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn run build:lib && yarn run build:package",
    "build:lib": "cd packages/async-lib && yarn run build",
    "build:package": "cd packages/async-package && yarn run build",
    "build:package:debug": "cd packages/async-package && yarn run build:debug"
  },
  "devDependencies": {},
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
